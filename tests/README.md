# Тесты

Тесты разнесены по доменам:

- `agent/` — тесты библиотеки агента, FastAPI-сервиса, хранилища и runtime.
- `emulator/` — тесты MCP-эмулятора таск-трекера.

Запуск из корня репозитория:

```bash
make test
```
