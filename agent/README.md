# Агент

Здесь находится Python-пакет `simple_agent`: библиотека агента, FastAPI-сервис, хранилище наблюдаемости, MCP-клиент к таск-трекеру и runtime-компоненты.

Локальный запуск выполняется из корня репозитория через `make run-agent`.
